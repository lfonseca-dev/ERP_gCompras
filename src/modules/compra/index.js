import StatusRouter from "./status/status.routes.js";

const CompraRoutes = [
    {
        module: "compra",
        path: "/status",
        router: StatusRouter
    }
];

export default CompraRoutes;